Nối MỘT project vào KB.

Chạy: `bash scripts/link-kb.sh <đường-dẫn-project>` với đường dẫn tôi đưa bên dưới
(nếu tôi không đưa, hỏi tôi đường dẫn project).

Việc này tạo symlink `.kb`, skeleton `.kb-local/`, `CLAUDE.md` trỏ sang KB, và slash
command — tôn trọng `CLAUDE.md` sẵn có. Sau khi chạy, tóm tắt kết quả.

$ARGUMENTS
